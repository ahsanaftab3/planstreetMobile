<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:controls="clr-namespace:PlanStreet.Controls;"
    xmlns:constants="clr-namespace:PlanStreet.Constants;"
    x:Class="PlanStreet.Views.TimeLineTopTabbedPage"
    BackgroundColor="{x:Static constants:AppThemeConstants.BackgroundColor}"
    Title="TimeLine">
    <ContentPage.ToolbarItems>
        <ToolbarItem Icon="ic_folder_selected.png" Command="{Binding CreateProjectCommand}" />
        <ToolbarItem Icon="ic_folder.png" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
            <controls:TabbedControl
            Margin="10,6,10,10"
                BackgroundColor="{x:Static constants:AppThemeConstants.BackgroundColor}"
                x:Name="tabbedControl"
                HorizontalOptions="FillAndExpand"
                VerticalOptions="FillAndExpand" />
    </ContentPage.Content>
</ContentPage>